#pragma once
#include <iostream>
#include <limits>

const size_t STARTING_MONEY = 100;
const size_t MONEY_BANK = 0;
const double INITIAL_DISTANCE = std::numeric_limits<double>::max();
const size_t MAX_DRIVERS = 1000;
const size_t MAX_SIZE = 1024;